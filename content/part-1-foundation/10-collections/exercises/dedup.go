package exercises

// Exercise 4: Order-preserving de-duplication.
//
// Return a new slice with duplicates removed, keeping the FIRST occurrence of
// each value in its original position. A map[int]bool (a "set") is the natural
// way to remember what you've already seen.
//
// Note: slices.Compact only removes ADJACENT duplicates, so it is not the answer
// here — "a b a" must become "a b", not "a b a".
//
// TODO: track seen values in a set; append a value the first time you meet it.
func Dedup(s []int) []int {
	// TODO: implement
	return nil
}
